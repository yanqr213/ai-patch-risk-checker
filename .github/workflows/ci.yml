name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.9", "3.10", "3.11", "3.12", "3.13"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install package
        run: python -m pip install -e .

      - name: Run tests
        run: python -m unittest discover -s tests -v

      - name: Run example check
        run: python -m ai_patch_risk_checker.cli analyze --diff examples/auth-without-tests.diff --format json

      - name: Run SARIF smoke
        run: |
          python -m ai_patch_risk_checker.cli analyze --diff examples/auth-without-tests.diff --format sarif --output patch-risk.sarif
          python -m json.tool patch-risk.sarif > /dev/null
          python -m ai_patch_risk_checker.cli analyze --diff examples/auth-without-tests.diff --format remediation --output remediation.md
          test -s remediation.md
          python -m ai_patch_risk_checker.cli analyze --diff examples/auth-without-tests.diff --format remediation-json --output remediation.json
          python -m json.tool remediation.json > /dev/null

      - name: Run baseline smoke
        run: |
          python -m ai_patch_risk_checker.cli baseline --diff examples/auth-without-tests.diff --output ai-patch-risk-baseline.json
          python -m json.tool ai-patch-risk-baseline.json > /dev/null
          python -m ai_patch_risk_checker.cli check --diff examples/auth-without-tests.diff --baseline ai-patch-risk-baseline.json --format json --output filtered-risk.json
          python -m json.tool filtered-risk.json > /dev/null
